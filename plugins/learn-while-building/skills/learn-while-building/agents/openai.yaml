interface:
  display_name: "Learn While Building"
  short_description: "Ship the code. Keep the understanding."
  default_prompt: "Use $learn-while-building in Quiet mode. Start meaningful updates with the maximum 30-second learning brief and its fixed end marker, then continue the normal response."
policy:
  allow_implicit_invocation: true
